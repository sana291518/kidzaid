<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Kidzaidssj" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash Screen Theme without Action Bar -->
    <style name="Theme.Kidzaidssj.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Main Theme without Action Bar -->
    <style name="Theme.Kidzaidssj.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Add your main activity specific styles here if needed -->
    </style>
</resources>
